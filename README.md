# SimpleApp

Minimal JSP app for Tomcat deployment.

## Files

- `index.jsp` – Main page
- `WEB-INF/web.xml` – Web deployment descriptor

## Deploy on Tomcat

1. Copy the folder into Tomcat's `webapps` directory:
```bash
cp -r simpleapp /opt/tomcat/webapps/
